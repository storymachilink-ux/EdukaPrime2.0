[build]
  command = "cd landing-page && npm install && npm run build"
  publish = "landing-page/dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Build environment variables (você ainda precisa configurar no painel do Netlify)
# VITE_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY